# Reject dependencies published in the last 24h — a compromised release is
# usually caught and unpublished well inside that window. CI's pnpm enforces
# this by default; setting it here keeps local resolution identical.
minimumReleaseAge: 1440

allowBuilds:
  esbuild: true
  sharp: true
